# Blogiz NextJs Website

## Installation:

1. Clone the repository.
2. Install dependencies using `npm install`.
3. Run the project using `npm run dev`.
4. To Run In built server use `npm run json-server`
